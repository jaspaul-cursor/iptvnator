---
area: web
type: breaking
---

IPTVnator is now a Progressive Web App only. The desktop Electron build, embedded MPV, download manager, SQLite-backed catalog, and related desktop-only settings have been removed. Use the web app with an optional self-hosted backend proxy, or the existing `mpv://` handoff for external playback.
