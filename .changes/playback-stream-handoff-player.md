---
type: feature
area: playback
---

The PWA playback settings now include a Stream handoff player. It shows copy and MPV actions for the stream URL and relay URL in the player area on every view, so you can hand off playback without waiting for an inline error.
